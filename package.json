{
  "name": "neon-demo",
  "version": "0.1.0",
  "description": "",
  "main": "index.node",
  "scripts": {
    "build": "rm -f index.node && cargo-cp-artifact -nc index.node -- cargo build --message-format=json-render-diagnostics",
    "install": "npm run build",
    "test": "cargo test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cargo-cp-artifact": "^0.1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "wasm-fibonacci": "^0.1.0"
  }
}
